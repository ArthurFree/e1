# AGENTS.md

面向 AI 编码代理的项目说明。本文件描述项目当前的真实状态，不推测未实现的细节。

## 项目概述

`notion-like-web` 是一个独立的 Web 笔记应用：以 Tiptap 的 Notion-like 模板为交互和视觉参考，提供本地优先（离线可用）的知识库、页面树和块编辑能力。面向简体中文个人用户。

**当前状态：第 1–5 阶段、R001–R006（含 C1–C5）、PR6 与 R007 均已完成；R008 进行中。** 各阶段完整细节见 `docs/requirements/`（R003–R008 规划）与 `docs/architecture/`；此处只保留代理日常工作所需的要点：

- **R001/R002（UI 设计系统与视觉重构）**：语义设计令牌（浅/深颜色、字体、圆角、阴影、z-index、动效 + `prefers-reduced-motion`，主色 `#22A06B`）、240px 全局侧栏（1024–1279px 折叠 64px）、开始首页/知识库首页/编辑器外框重构、统一 `ui/Dialog` 与 `ui/EmptyState`、`ui/icons.tsx` 55 枚 SVG 系统图标（系统 Emoji 图标清零，用户自定义图标仍允许 Emoji）、页面树键盘导航、Cascade Layers 样式结构。
- **R003（架构整改，八阶段全完成）**：`SaveCoordinator` 保存系统（每文档串行队列 + 代次管理 + 恢复缓冲）、`WorkspaceSessionService` 会话原子加载（requestId 丢弃过期响应）、偏好事务化（串行队列 + 防抖）、正文 JSON 白名单校验 + 损坏面板、统一 `DomainError` 错误码、页面/标签关系不变量、`AppServices` 服务容器 + 依赖注入、AppState 拆分四状态域 + 渲染隔离、IndexedDB v3 索引优化、`docs/architecture/` 六主题文档 + `application/devDiagnostics.ts`（仅 dev 启用）。
- **R004（数据一致性，全完成）**：`DocumentWriteRepository` 原子文档写、`DocumentCommitService` 正文提交单点、INV-01~07 不变量（`docs/architecture/document-write-path.md`）、写入路径绕过清零（模板/AI/导入/版本恢复全部走统一通道）、AppState 拆为 Preferences → Workspace → Navigation → Overlay 四 Provider、IndexedDB v4（workspaceId 回写）、附件空间治理（20MB/100MB/MIME 白名单 + 配额错误独立文案）、多标签页（BroadcastChannel + 正文乐观锁 DOCUMENT_CONFLICT + 冲突面板）、工程门禁（ESLint/Prettier/dependency-cruiser/`npm run ci`/GitHub Actions）、Workspace/Navigation Context 细分 Data/Command 双片。内容寻址快照（§6.5）未做，见 `docs/requirements/r004.md`。
- **R005（Web 优先与双端准备，阶段 0–8 完成）**：业务编排移出 React Provider——`application/commands/` 四命令服务 + `application/queries/` 三查询服务；Bootstrap 拆分（`bootstrap/mountApplication` + `platform/web/createWebRuntime` + `main.web.tsx` 唯一 Web 装配根）；`RuntimeCapabilities` 能力矩阵（DUAL-01）；AssetStore 资源抽象（Attachment 去 Blob，Blob 只在 `platform/web` 适配边界）；Portable Vault `.e1.zip` 导入导出；不透明版本令牌 `ContentVersionToken`；持久化级 MarkdownCodec（`src/editor/markdown/`）；SearchIndexPort + 会话去正文；RecoveryStore/ChangeChannel/SecretStore/AIConfigService/StorageHealthService 平台服务 port（AI apiKey 剥离至 SecretStore，IndexedDB v5）。阶段 9/10（Electron 验证、SQLite/文件监听）由 R006 接替，见 `docs/requirements/r005.md`。
- **R006（Electron 桌面端，C0–C5 完成）**：`electron/` 主进程（ESM `dist-electron/main.mjs`）+ 预加载（sandbox 必须 CJS `dist-electron/preload.cjs`）；`shared/` 共享 IPC 契约（channel/类型/schema 校验/错误码双向映射）；Vault 文件系统层（PathGuard 符号链接防护、VaultFileSystem、NoteFileSystem 10MiB/UTF-8 严格解码/SHA256 versionToken、AtomicFileWriter、AssetFileSystem）；授权边界收口（`vault:open` 已删，改 selectionToken 一次性令牌 + openRecent，Renderer 不见 absolutePath；未初始化目录仅预览不自动初始化）；安全阅读闭环（`openDocument` 模型 DocumentOpenResult{content,access,compatibility,source}、只读切面、兼容 Banner、统一错误块）；Markdown 创建与安全保存（`DesktopMarkdownWriteService` 写入单点、Stable ID Adoption、`DesktopIdentityAliasRegistry`）；本地附件闭环（`e1-asset://` 协议、pickToken、persistentAssetPaths）。
- **PR6（Web 持久化平台化）**：IndexedDB 生产实现迁入 `src/platform/web/persistence/`，装配根为 `platform/web/createBrowserServices.ts`，与 `platform/desktop/` 对称；`src/infrastructure/` 只留平台无关部分。纯分层依赖规则统一由 `.dependency-cruiser.js` 强制，`src/test/architecture.test.ts` 只保留行为不变量。
- **R007（Desktop 产品化，阶段 0–4 完成）**：基线全绿；修复重启后打开含图文档误报「图片不可用」——`DocumentEditor` 的 `storage.assetServices` 注入提前至 `onBeforeCreate`（初始内容节点视图随 EditorView 同步装配，useEffect 注入太晚）。阶段 1（Metadata 写入）：`note.patchMetadata` IPC（只改 Frontmatter title/tags，乐观锁 + 原子写）、`DesktopNoteMetadataService` 编排（rename/setPageTags 接通）、`DocumentVersionChannel` 版本推进防假冲突、标签色确定性派生。阶段 2（设备级交互状态）：`vaultState.get/patch` IPC + `electron/main/state/DesktopVaultStateStore`（`userData/vault-state/<vaultId>.json`，损坏自愈同 VaultRegistry 口径；transient 不落盘）、`DesktopVaultStateClient`（会话缓存 + Adoption 键「读兜底 + 写清空」迁移）、workspace/page 的 setFavorite 与 page setLastOpened 接通，收藏/最近重启保持且不改 Markdown。阶段 3（External Change Watcher）：`electron/main/watcher/`（chokidar + 150ms coalescing 超阈值降级 rescan-required + SelfWriteRegistry 自写回声抑制，create/save/patchMetadata/asset.import 成功后登记，scan 后启动监听）、首个 Main→Renderer 单向事件通道 `events:vaultChanges` + preload `events.subscribeVaultChanges`、`application/services/ExternalVaultChangeService` 契约 + `platform/desktop/DesktopExternalVaultChangeService`（200ms 静止窗口 → scan 旧快照/rescan 新快照 → stable-id diff → 归一化 created/modified/moved/deleted，asset/rescan-required 只触发重扫不产生文档变更）、`AppServices.externalVaultChanges` 可选字段、`components/shell/ExternalVaultChangeBridge`（fileWatching 门控，命中当前 vaultId 即 refreshCurrentWorkspace）、当前文档策略（clean 自动重载 + 轻量提示 / dirty 冲突面板 / 外部删除错误块或保留内存另存副本 / 同 stable id 重建复苏；`DocumentScreen` 幽灵页保活防树刷新后落空态）、moved 前同步 Source Cache 路径、`desktopCapabilities.fileWatching` 翻 true。阶段 4（文件操作闭环）：Main `VaultFileOperations`/`VaultTrashFileSystem`（`.e1/trash/<operationId>/{payload,meta.json}`，删除一律 rename 不 unlink，restore 父目录递归重建 + 冲突确定性改名）+ `vault.createDirectory/trash/listTrash/restore/purgeTrash` 与 `note.move/renameFile` IPC；Renderer `DesktopPageRepository` 全接通（create 分组=真实目录、remove 文档/分组进回收站、restore/purge 经 `trash:<vaultId>/<operationId>` 页面 id 反解、move 仅 document→directory 且同步 Source Cache 路径防写回旧路径）、`listByWorkspace` 合并回收站条目（标题取原文件名，TrashPanel 零改动）、每次写后 scans.invalidate；`RuntimeOperations` 操作支持矩阵（§9，`src/runtime/` + `AppServices.operations`，Capability=平台能力/Operation=产品允许的操作分离）——Web 全 true，Desktop workspace.rename/page.renameFile/revision.read/write=false，PageTreeSidebar 行内动作与 EditorShell 版本历史入口按矩阵隐藏。偏差：P1 分组改名/移动因 Main 无目录 IPC 诚实 NOT_IMPLEMENTED；P2 renameFile 未接线（置 false）。原阶段 5（Secret/Reveal）未启动，已迁移至 R008 Stage 1–2；R007 至此关闭（P1/P2 偏差由 R008 Stage 0 以操作矩阵细分收口，group rename/move 入口不再暴露）。
- **R008（Desktop 产品化收尾与搜索规模化，Stage 0–3 完成，实现中）**：Stage 0（R007 遗留一致性收口）——`RuntimeOperations` 的 page 组按 R8-01 细分 `document{create,renameTitle,renameFile,move,trash,favorite}` / `group{create,rename,move,trash}` / `trash{restore,purge}`（Desktop：document 除 renameFile=false 全 true、group.rename/move=false、trash 全 true；Web/内存全 true），`PageTreeSidebar` 行内重命名/拖拽/F2/新建分组自动改名均按对象类型门控，unsupported 入口不存在而非点了报错；chokidar 迁入 production dependencies，`scripts/build-electron.test.mjs` external 依赖门禁 + CI `desktop-runtime-deps` job（prune --omit=dev 后逐个 external import 验证）。Stage 1（Native Secret Store）——`electron/main/secrets/`（`SecretBackendStatus` 三档判定 secure-persistent/session-only/unavailable + `SecretFilePersistence` userData/secrets.json 密文 `{version:1,entries}` 损坏备份自愈/unknown version 不覆盖 + `DesktopSecretStore` 编排，safeStorage 优先 encryptStringAsync/decryptStringAsync 退回同步版，不安全 backend 如 basic_text 降级 session-only 仅内存绝不落盘，`E1_SECRET_BACKEND_FORCE` 为 G11 注入点）；`secret.get/set/remove/getStatus` IPC（name 白名单 + value 8KiB 上限 schema，错误码新增 SECRET_STORAGE_UNAVAILABLE 不映射 DomainError）；Renderer `DesktopSecretStore` 替换 InMemorySecretStore + `SecretStorageStatus` 平台无关类型 + SecretStore port 可选 `getStatus()`（R8-02：capability.nativeSecrets 翻 true 表示接入 native 体系，运行态由 status 表达）；SettingsPanel 按 status 分流 API Key 保存文案（secure-persistent/session-only/unavailable/Web 回退 IndexedDB），禁 localStorage fallback；E2E `desktop.secrets.spec.ts` G10（重启保持，无安全 backend 环境条件跳过）/G11（basic_text 注入→不落盘→重启丢失）。Stage 2（Reveal in File Manager）——`note.reveal`/`asset.reveal` IPC 共用 `RevealInput{vaultId, relativePath}`（附件经会话资源索引反查 relativePath，不走 assetId；R8-07 Renderer 绝不传 absolutePath），Main `ipc/reveal.ts` 授权边界 → PathGuard（realpath 根内，目标必须存在，NOTE_NOT_FOUND 复用不新加码）→ `shell.showItemInFolder`，只读操作 transient 也允许；Renderer 可选 port `RevealService`（`AppServices.revealService`，Desktop 实现经 SourceCache/AssetRegistry 反查后走桥）；EditorShell 顶栏「在文件管理器中显示」+ 附件块「定位」按钮（capability + port 双门控，Web 天然无入口），`revealInFileManager` 翻 true；E2E `desktop.reveal.spec.ts` G12/G13（evaluate stub shell.showItemInFolder 断言链路，真实 GUI 人工验收）。Stage 3（Search Contract + Benchmark）——`src/application/services/SearchContract.ts` 冻结 `SearchDocument`/`SearchResult`/`SearchIndexStatus` 与新 port `FullTextSearchIndexPort`（prepareWorkspace/search/upsert/remove/rebuild/getStatus；既有标题搜索 `SearchIndexPort` 及其三实现不动，新旧并存），查询语义可执行化（`rankSearchDocuments`：trim/大小写不敏感/Unicode，权重 exact title 100 > prefix 80 > contains 60 > tag 45/40 > body 20，同分稳定排序，limit 上限 100）；`shared/markdown/searchText.ts` `markdownToSearchText`（Markdown→可检索纯文本，Main/Renderer 共用）；内存参照实现 `InMemoryFullTextSearchIndex` + 契约套件 `src/test/searchIndexContract.ts`（14 组断言含验收语料，Stage 4 Desktop 实现复跑）；`fixtures/search/`（mulberry32 固定 seed 生成器覆盖 §10.7 全维度 + 16 条固定 query 验收语料）与 `src/test/searchIndex.perf-wallclock.test.ts`（§18 JSON 基线，内存占位，Stage 4 复用同一 harness）；中文方案倾向 B（FTS bigram 召回 + 契约层精排），Stage 4 实测确认。Stage 4–6（全文索引 / 增量索引 / 规模验收）未启动，见 `docs/requirements/R008-desktop-productization-search-scale.md`。

**质量**：1200 单元/组件测试（含性能基准、IndexedDB v1–v5 迁移、DocumentWrite/正文乐观锁/AssetStore/RecoveryStore/SecretStore 两实现契约套件、多标签页同步、Context 渲染粒度、桌面 IPC/PathGuard/Vault/Watcher/文件操作套件）、47 Playwright + 37 桌面 E2E、typecheck、lint、deps:check、生产构建全部通过。原「已知本地失败」3 例（`desktop.assets` E2E-03/04、`desktop.saving` 新建文档）已根治——根因为本机 node_modules 缺 chokidar、contextBridge 跨桥错误码丢失（真实产品缺陷，已用桥编码修复）与两处测试自身选择器/选区问题，详见 R007 文档阶段 4 记录。视觉基线 macOS 生成不进 CI。常用命令：`npm run dev` / `build` / `test`（Web 显式别名 `dev:web` / `build:web` / `test:web`）、`npm run typecheck` / `lint` / `deps:check` / `ci`、`npm run test:e2e`（更新基线 `test:e2e:update`）、`npm run dev:desktop` / `build:desktop` / `test:desktop` / `test:e2e:desktop`。

## 源码结构

- `src/domain/`：实体类型（`types.ts`）、页面树纯逻辑、全局搜索、AI、活动列表、创建位置选择、路由持久化、版本策略（含 interval 5MB 字节预算）、字数统计、仓储接口（`repositories.ts`，含 `DocumentWriteRepository` 原子文档写与正文乐观锁 expectedVersion）、`errors.ts`（DomainError 稳定错误码）、附件统一校验、多标签页事件契约（`sync.ts`）、正文 JSON 白名单校验（`validation/documentContent.ts`，与 extensions.ts schema 同步由测试强制）。
- `src/infrastructure/`：**只保留平台无关基础设施**——`aiProvider.ts`（OpenAI 兼容 HTTP provider，双端装配根共用）、`id.ts`（ULID）、`memory/`（内存仓储：全部 port 纯内存实现 + RecoveryStore/SecretStore/StorageHealth 内存实现 + `createInMemoryAppServices`，可替换性证明；R008 Stage 3 起含全文搜索契约参照实现 `InMemoryFullTextSearchIndex`）。
- `src/editor/`：编辑器内核——`extensions.ts`、`markdown/`（持久化级 MarkdownCodec：codec/frontmatter/links/serialize/sourceScan）、`commands.ts`（统一命令注册表）、`aiBridge.ts`、slash/mention suggestion、toc、blockActions、tableUtils、format、indent、codeBlock、`attachment.ts` 与 `localImage.ts`（附件/图片节点经 `editor.storage.assetServices` 注入，无浏览器 API 与仓储直写）、`templates.ts`。
- `src/application/`：应用服务层——`AppServices.ts`（服务容器接口：`commands{workspace,page,tag,document}` + `queries{workspace,document,search}` + `preferencesService` + `assets` + `createAIProvider`/`createSaveCoordinator` + `syncChannel`/`recoveryStore`/`secretStore`/`aiConfigService`/`storageHealth` + `capabilities`/`operations`（R007 §9 操作支持矩阵） + 可选 `vaultMaintenance`/`documentSafety`；原始仓储与 documentCommit/session/searchIndex 不公开）、`commands/`（业务写编排：Workspace/Page（含回收站）/Tag/Document，承载仓储写 + 搜索索引同步 + 跨标签页广播）、`queries/`（只读编排；`DocumentQueryService.openDocument` 打开模型，Web 默认 editable/lossy:false）、`services/`（`SaveCoordinator` 保存串行队列 + 代次 + 乐观锁 + 维护任务只跟随当前代次、`DocumentCommitService` 正文提交单点、SearchIndexPort（标题搜索）、SearchContract（R008 Stage 3 全文搜索契约 + `FullTextSearchIndexPort` + 可执行查询语义）、RecoveryStore、ChangeChannel、SecretStore/AIConfigService/StorageHealthService、`WorkspaceSessionService` 会话原子加载（不读正文）、`PreferencesService` 串行队列 + dispose、`corruptedDiagnostics`（application 唯一 localStorage 例外））、`assets/`（AssetCommandService 导入/删除编排 + AssetAccessService/AssetPicker/NotificationService 接口）、`markdown/`（导出资源还原）、`services/zip.ts`/`zipReader.ts`（最小 ZIP 读写）、`vault/`（VaultExportService/VaultImportService）、`devDiagnostics.ts`。
- `src/bootstrap/`：`mountApplication.tsx` 共享挂载入口（不得 import infrastructure）。
- `src/runtime/`：`RuntimeCapabilities.ts` 能力矩阵接口（DUAL-01：组件只判断能力字段、不得判断平台名称；字段语义见 `docs/architecture/runtime-boundaries.md`）+ `RuntimeOperations.ts` 操作支持矩阵接口（R007 §9：UI 是否显示操作入口，与平台能力分离）。
- `src/platform/web/`：Web 平台边界——`createWebRuntime.ts`、`createBrowserServices.ts`（生产服务装配根）、`persistence/`（IndexedDB：`db.ts` DB v5 + 按 oldVersion 分支迁移 + 连接生命周期处理、`repositories.ts`（软删/恢复、版本、assetStore、purge 级联、关系约束、索引查询、原子文档写、正文乐观锁、偏好新形状校验）、`secretStore.ts`、`seed.ts` + 随行迁移/契约/性能测试）、`webCapabilities.ts`、资源适配器（`webAssetAccess.ts`/`webAssetPicker.ts`/`webNotification.ts`——**Blob 只存在于该适配边界**）、`search/BrowserMemorySearchIndex`、`webRecoveryStore.ts`（localStorage）、`BroadcastChangeChannel.ts`、`webStorageHealth.ts`（StorageManager 只在此边界）；`src/main.web.tsx` 为唯一 Web 装配根（仅它与 platform/web 可 import createBrowserServices）。
- `src/state/`：`AppServicesProvider.tsx`（容器注入，组件经 `useAppServices()` 取服务，禁止直接 import infrastructure）、`AppProviders.tsx`（Preferences → Workspace → Navigation → Overlay 组合）、四个状态域 Provider（各细分 Data/Command 双 Context，命令回调引用恒定）、`workspace/sessionReducer.ts`、`legacy/useApp.ts`（兼容聚合门面，仅供既有测试）。**生产代码一律用细粒度 hook**（useWorkspaceData/useWorkspaceCommands/useNavigationState/useNavigationCommands/usePreferences/useOverlay），组件使用聚合 hook 由架构测试拦截。
- `src/components/`：`AppShell`、`shell/GlobalSidebar`（能力门控「打开本地知识库」入口）、`shell/VaultConfirmDialog`（Vault 三选项确认框）、`shell/ExternalVaultChangeBridge`（R007 阶段 3：fileWatching 门控的外部变更→页面树刷新桥，挂载于 AppProviders 内 App.tsx）、`ui/`（icons 55 枚 + PageIcon/Button/IconButton/Dialog/EmptyState）、`PageTreeSidebar`、`MainArea`（按视图分发；openDocument 打开链路——兼容 Banner/Stable ID Adoption/lossy 保存确认/统一错误块）、`StartPage`、`ActivityList`、`RecentPage`、`FavoritesPage`、`WorkspaceHome`（Desktop 门控「重新扫描」）、`TargetPicker`、`TemplateCenter`、`AIDraftModal`、`CreateWorkspaceModal`、`SearchPanel`、`TrashPanel`、`SettingsPanel`、`VersionPanel`、`TagPicker`、`TitleEditor`、`StartPreview`；`src/components/editor/`：`DocumentEditor`（宿主 + `access` prop 只读切面）、`FormatToolbar`、`BubbleToolbar`、`AIAssistantPanel`、`SaveStateIndicator`、`WordCount`、`TableToolbar`、`BlockHandle`、`CommandList`、`EmojiPicker`、`TocPanel`。
- `src/hooks/useDebouncedCallback.ts`：防抖保存 hook（beforeunload/卸载时 flush）。
- `electron/`：Electron 壳——`main/main.ts`（生命周期 + registerIpcHandlers）、`main/window.ts`（1440×900，sandbox/contextIsolation 开启，dev 加载 `${E1_DEV_SERVER_URL}/desktop.html`、prod 加载 `dist/desktop.html`）、`main/ipc/`（统一包装「schema 校验→分发→IpcResult 信封，handler 永不 throw」+ vault/vaultState/note/asset/secret/reveal 六组）、`main/filesystem/`（PathGuard/VaultFileSystem/NoteFileSystem/AtomicFileWriter/AssetFileSystem）、`main/state/DesktopVaultStateStore.ts`（userData/vault-state 设备级交互状态，R007 阶段 2）、`main/secrets/`（DesktopSecretStore/SecretFilePersistence/SecretBackendStatus，safeStorage 安全存储 + 安全降级，R008 Stage 1）、`main/watcher/`（VaultWatcher/VaultWatcherService + WatchEventCoalescer + SelfWriteRegistry，chokidar 外部变更监听与自写回声抑制，R007 阶段 3）、`main/vaultRegistry.ts`（最近 Vault 注册表）、`main/SelectionTokenStore.ts`/`transientVaults.ts`/`vaultRoots.ts`（授权边界）、`preload/preload.ts`（contextBridge 暴露 `window.e1` E1DesktopAPI；sandbox 下跨桥错误会被重建为 plain Error、自定义属性丢失，preload 把错误载荷编码进 message（shared/errors `encodeIpcBridgeError`），Renderer 经 `desktopApi.getDesktopApi()` 解码还原为带 code 的 DesktopIpcError）；esbuild 构建为 `dist-electron/main.mjs`（ESM）+ `preload.cjs`（CJS）。
- `shared/`：Renderer 与 Main/Preload 共享契约（**零依赖、环境中立、不得 import src/electron**）——`errors.ts`（IPC 错误码 + DomainError 双向映射 + 可选 details）、`ipc/contracts.ts`（channel 常量 + vault/vaultState/note/asset 请求响应类型 + VaultFsEvent 单向事件 + E1DesktopAPI + IpcResult 信封）、`ipc/schemas.ts`（手写运行时校验器，INVALID_INPUT/PATH_ESCAPE）、`markdown/frontmatter.ts`（Main 扫描与 MarkdownCodec 共用同一实现，含 `ensureFrontmatterId`）、`markdown/searchText.ts`（R008 Stage 3：Markdown→可检索纯文本，Main/Renderer 共用）。
- `src/platform/desktop/`：Desktop 平台边界——`desktopApi.ts`（仅它与 main.desktop.tsx 可触碰 window.e1/getDesktopApi）、`desktopCapabilities.ts`、`vaultMapping.ts`/`repositories.ts`/`stubRepositories.ts`/`preferencesRepository.ts`、`DesktopMarkdownWriteService.ts`（写入单点：save/replaceContent 统一 Source/Identity/Output Gate）/`DesktopIdentityAliasRegistry.ts`/`DesktopDocumentSourceCache.ts`/`DesktopAssetStore.ts`/`DesktopAssetRegistry.ts`/`DesktopAssetPicker.ts`/`DesktopAssetAccessService.ts`/`DesktopMarkdownAssetHydrator.ts`、`vaultOpenConfirmation.ts`（确认握手单例）、`DesktopExternalVaultChangeService.ts`（R007 阶段 3：Watcher 事件 reconciliation，订阅 events.subscribeVaultChanges）、`DesktopRevealService.ts`（R008 Stage 2：RevealService port 实现，SourceCache/AssetRegistry 反查 relativePath 走桥）、`createDesktopRuntime.ts`；`src/main.desktop.tsx` 为 Desktop 装配根（desktop.html 指向）。
- `scripts/`：`build-electron.mjs`（Electron 产物 esbuild 构建）、`build-electron.test.mjs`（external 依赖门禁：除 electron 外必须在 production dependencies 且可解析，R008 §7.4）、`dev-desktop.mjs`（vite + esbuild watch + spawn Electron 编排，信号联动清理）。
- `src/test/`：Vitest 共享底座——`setup.ts`、`fixtures.ts`、`renderProbe.tsx`、`TestApp.tsx`、DocumentWrite/正文乐观锁/AssetStore/RecoveryStore 两实现契约套件、`assetTestServices.ts`、`architecture.test.ts`（**只做模块解析看不见的行为不变量**：components 禁聚合 hook 与直写仓储、components/state 不得 import domain/repositories、不得访问 AppServices 已删字段、editor 不得 import domain/repositories、装配根白名单、bootstrap 不得 import infrastructure、window.e1/getDesktopApi 白名单、src 禁 isElectron/process.platform/process.versions.electron 字面量（DUAL-01）；纯分层依赖规则统一由 `.dependency-cruiser.js` 强制）。
- `src/styles/`：Cascade Layers——`index.css` 入口 + `tokens.css`（语义设计令牌）+ reset/typography/global + `components/`；应用壳网格行高固定 `minmax(0, 1fr)` 且 `overflow: clip`；`.doc-layout` 纵向，`.doc-main` 为正文 + 目录行容器。
- `e2e/`：Playwright——`app.spec.ts`（18 例功能端到端）、`multi-tab.spec.ts`（双标签页冲突不静默覆盖）、`visual.spec.ts` + `visual-pages.spec.ts`（1440×900 基线 + 四档宽度 + 深色）、`responsive.spec.ts`、`desktop.*.spec.ts`（冒烟/安全阅读/创建保存/附件闭环，`_electron.launch` prod 模式需先 `build:desktop`，经 grep「桌面冒烟」与 Web 套件分离）；浏览器二进制装在项目内（`PLAYWRIGHT_BROWSERS_PATH=0`）。
- 工程门禁：`eslint.config.js`（flat，0 error）、`.prettierrc`、`.dependency-cruiser.js`（no-circular + 分层 forbidden，测试文件豁免）、`.github/workflows/ci.yml`（quality/build-web/build-desktop/e2e-web/e2e-desktop；视觉基线 macOS 生成不进 CI）。

## 技术栈（已确认的决策）

- React + Vite + TypeScript：单页 Web 应用；Electron 桌面端（R006 起）。
- Tiptap 3：富文本编辑内核，只使用开源扩展，不引入 Tiptap Pro 专有能力；Markdown 导入导出用官方 `@tiptap/markdown`。
- IndexedDB：Web 端本地数据与二进制资源持久化，封装为仓储层；Desktop 端为文件系统 Vault（`.e1/vault.json` + Markdown + assets/）。
- CSS variables + 模块化样式：主题令牌与组件样式。
- Vitest + Testing Library：单元与组件测试；Playwright：端到端与截图回归。

首版不做：注册/登录、云同步、多人协作、评论、权限、支付。

## 文档结构（唯一的内容来源）

- `README.md`：功能、运行/测试命令、架构概览、隐私说明与已确认决策。
- `docs/requirements.md`：产品需求、功能范围与验收标准。
- `docs/architecture.md`：架构文档索引；主题文档在 `docs/architecture/`（overview/dependency-rules/state-management/persistence/editor-save-pipeline/error-handling/document-write-path/runtime-boundaries），重大决策 ADR 在 `docs/adr/`，数据库迁移说明在 `docs/migrations/`。
- `docs/ui-spec.md`：页面结构、视觉状态、响应式规则与可访问性要求；基准视口 1440 × 900。
- `docs/test-plan.md`：单元、组件、端到端与视觉回归测试计划及发布前验收。
- `docs/implementation-plan.md`：五个实现阶段及各阶段完成标准。
- `docs/decisions.md`：决策记录表；**任何改变这些结论的需求，须先更新本表及受影响的文档，再改代码。**
- `docs/requirements/R003.md`、`r004.md`、`r005.md`、`r006-c3.md` 等：各整改批次的实施规划与完成状态。

## 开始实现时的架构约束

- 界面只依赖仓储接口和领域状态；Tiptap 仅通过编辑器适配层与页面内容交互，基础设施层（IndexedDB、Markdown 转换、AI provider）可整体替换。
- 文档内容 JSON 是唯一编辑真相；`textSnapshot` 仅用于搜索与 Markdown 导出。
- 统一命令定义驱动命令菜单、浮动工具栏和块菜单，避免三处功能分叉。
- 编辑器变更经防抖后保存；切换文档或卸载页面时强制落盘。
- 实施顺序约束（见 `docs/implementation-plan.md` 末尾）：仓储与领域模型先于编辑器保存；命令注册先于各类菜单；表格、拖拽、AI 在基础编辑器稳定后引入；每阶段完成后才更新视觉基线。

## 构建与测试

已有命令：`npm run dev`（别名 `dev:web`）、`npm run build`（类型检查 + 生产构建，别名 `build:web`）、`npm test`（Vitest，jsdom + fake-indexeddb，别名 `test:web`）、`npm run test:perf`（wall-clock，不进 CI）、`npm run typecheck`（Web + Electron 双 tsconfig）、`npm run lint`（0 error 门禁）、`npm run format:check` / `format`、`npm run deps:check`、`npm run test:coverage`、`npm run ci`（统一门禁：typecheck + lint + deps:check + test，不含 build/E2E）、`npm run test:e2e` / `test:e2e:update`、`npm run test:e2e:desktop` / `test:e2e:desktop:golden`。测试体系现状：

- 单元测试覆盖页面树、仓储（含 IndexedDB v1–v5 迁移、损坏数据降级、正文乐观锁与 DocumentWrite 两实现契约套件）、搜索、Markdown 导入导出、AI 配置校验与错误映射、附件校验与版本空间预算、桌面 IPC/PathGuard/Vault 文件系统。
- 组件测试覆盖菜单键盘操作、格式化、撤销重做、自动保存防抖、刷新后状态恢复、保存后半程与版本恢复竞态、多标签页同步与冲突面板、状态域 Provider、Desktop 打开链路。
- Playwright 在 1440 × 900 固定视口做截图回归，保存中文基线并对动态光标、时间、随机 ID 做屏蔽；另在 1024px、768px、375px 做响应式冒烟；桌面 E2E 用 `_electron.launch` prod 模式。
- CI（GitHub Actions）：quality（`npm run ci` + 覆盖率）、build-web、build-desktop、e2e-web、e2e-desktop（xvfb + `@golden` 黄金路径）；视觉基线为 macOS 生成不进 CI。
- 发布前须通过全部自动化测试、类型检查、lint、deps:check 和生产构建。

## 约定与安全要求

- 默认语言为简体中文：UI 文案、预置示例文档、截图基线均使用中文。
- API key 和 endpoint 只存本地（Web：IndexedDB SecretStore；不进入同步、日志、分析或错误上报）；未配置 AI 时不发起任何外部请求，AI 输出须经用户确认后才写入文档。
- 图片、Markdown 导入和 AI 返回内容都经编辑器白名单解析，禁止把原始 HTML 直接注入 DOM。
- 可访问性：图标按钮提供 `aria-label`，菜单支持方向键、Enter、Escape，保证可见焦点状态与文本对比度。
- 视觉基准为 1440 × 900 桌面视口下的高还原度；窄屏（768px、375px）保持可用即可。
